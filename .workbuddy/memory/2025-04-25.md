## 2025-04-25

### 六级单词导入
- 从 `F:/4 六级-乱序.txt` 导入 **5651 个六级单词**到背单词小程序
- 创建了 `src/data/cet6Words.ts` 存储六级原始数据（word/phonetic/pos/meaning 格式）
- 修改 `src/data/words.ts`，添加 cet6Words 导入、转换逻辑和去重合并
- 六级单词难度统一设为 `hard`，释义格式为 `{词性} {中文释义}`
- 去重机制：与 baseWords 和 cet4Words 都做了去重，避免重复单词
